{
  "name": "harmonic-analysis",
  "version": "1.0.0",
  "description": "Harmonic Analysis Engine",
  "main": "src/harmonic-analysis/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:oracle": "vitest run tests/oracle",
    "test:arch": "vitest run tests/architecture",
    "typecheck": "tsc --noEmit",
    "check-deps": "npx ts-node scripts/check-dependency-rule.ts",
    "validate": "npm run typecheck && npm run check-deps && npm run test"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.25",
    "@types/node": "^20.0.0",
    "@types/supertest": "^6.0.2",
    "supertest": "^6.3.4",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
