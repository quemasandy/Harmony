---
name: speckit-validate
description: Run all tests and validations for the project
compatibility: Requires spec-kit project structure with .specify/ directory
metadata:
  author: custom
  source: custom
---

# Validate Changes

Runs all tests and validations (unit, oracle, arch, typecheck, check-deps) in a single command.

## Execution

Run the following command in the project root:

```bash
npm run validate
```
